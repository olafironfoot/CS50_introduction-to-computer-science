/**
 * make a program that writes and creates a text file that updates the time constantly
 * built recurssive statement to keep updating file name
 **/

#include <stdio.h>
#include <stdlib.h>

int recurssion(int);

int main(void)
{
    //build in recurssion
    if(recurssion(n) < 2);
    {
        printf("programs halt\n");
        return 0;
    }
    
    //open a file    
    FILE* outTime = fopen("outTime.txt", "w");
    
    //get time
    char * ctime(const time_t * timep);
    
    
    //write time into file
    
}